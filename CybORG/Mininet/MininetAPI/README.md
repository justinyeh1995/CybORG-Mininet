The scripts in this folder will all be run while user is root. This is because the mininet commands require root access. \\

Please install the following packages before running the scripts: \\
- mininet
